#ifndef OPERACIONES_H
#define OPERACIONES_H

/*
 * operaciones.h — Declaraciones de la biblioteca de operaciones
 *
 * Pre de todas las funciones: los argumentos son enteros válidos.
 */

/* sumar: devuelve a + b */
int sumar(int a, int b);

/* restar: devuelve a - b */
int restar(int a, int b);

/* multiplicar: devuelve a * b */
int multiplicar(int a, int b);

/* esPar: devuelve 1 si n es par, 0 si es impar */
int esPar(int n);

#endif
